// This file is intentionally empty — ComposeModal was replaced by ComposeView.
// The export below keeps any stray static imports from breaking the build during
// the transition; once all callers have been updated this file can be deleted.
export { default } from './ComposeView'
